// Maybe here we can declare the style for each theme, not just the classname for the element
const themes = {
  default: "light-theme",
  light: "light-theme",
  dark: "dark-theme"
};

export default themes;
